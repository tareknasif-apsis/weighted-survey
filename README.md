# mnext-frontend (demo)

1. npm install
2. npm run dev
3. Open http://localhost:3000 on a phone or mobile emulator

Notes:
- This is a frontend-only demo using localStorage and static data in /data/scenarios.ts
- All scoring and server-side validation are NOT implemented in this demo. Replace localStorage persistence with backend API endpoints when you add the server.
